services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-v0-web
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: "production"
      PORT: "3000"
    restart: unless-stopped
